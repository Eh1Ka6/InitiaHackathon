{
  "name": "weezdraw",
  "version": "1.0.0",
  "private": true,
  "description": "WeezDraw — Telegram social wagering on Initia",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "contracts:compile": "npm run compile --workspace=packages/contracts",
    "contracts:test": "npm run test --workspace=packages/contracts",
    "backend:dev": "npm run dev --workspace=packages/backend",
    "bot:dev": "npm run dev --workspace=packages/bot",
    "miniapp:dev": "npm run dev --workspace=packages/miniapp"
  }
}
